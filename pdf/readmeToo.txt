1.1 -> tip grila //se scrie doar textul, exemplu:
		//1. Variabila s memoreaza... ... ?
		// se va scrie:"Variabila s memoreaza... ... ?"
		//sau la variante:
		//b. strlen(s,2)=0
		//se scrie "strlen(s,2)=0" in casuta cu raspuns2
1.2.a -> standard
1.2.b -> standard //atentie!! raspunsuri multiple le separati cu un spatiu
1.2.c -> program //!!copiati la input valorile date la 1.2.a
		//sa fie separate cu un spatiu
		//nu scrieti numele variabilei=123 scrieti doar 123
1.2.d -> NU (de scris pseudocod)
====================================
2.1 -> tip grila
2.2 -> tip grila
2.3 -> standard
2.4 -> standard
2.5 -> program
====================================
3.1 -> tip grila
3.2 -> standard //aveti grija la formatarea textului
		//cand aveti program alaturat
3.3 -> program
3.4.a sau 3.4.b -> program 

// la toate programele se scriu doar acelea
// la care zice clar
// sa se scrie un program c/c++
// nu scrieti alea care zic
	*sa se scrie un subprogram
	*sa se scrie un antet
	*sa se scrie definitia unui nu stiu ce
// doar daca e clar ca aveti ce sa treceti la input
// si daca e clar pentru ala care rezolva ca trebuie sa faca un program